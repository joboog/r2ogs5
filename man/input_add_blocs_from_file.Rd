% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ogs5_read_inputfiles.R
\name{input_add_blocs_from_file}
\alias{input_add_blocs_from_file}
\title{Add ogs5 blocs directly from existing inputfile}
\usage{
input_add_blocs_from_file(
  ogs5_obj = NULL,
  sim_basename,
  filename,
  file_dir = "",
  overwrite = FALSE
)
}
\arguments{
\item{ogs5_obj}{Instance of class \emph{ogs5}. If set to \strong{NULL}, a new ogs5
instance is created with filename and file_dir as simulation name and path
respectively.}

\item{sim_basename}{(\emph{character}) The simulation name, usually the filename of
the input files without extension.}

\item{filename}{(\emph{character}) Can be the name of the file (with extension) to be
read in, a list of file names or "all".}

\item{file_dir}{(\emph{character}) A path to the directory where the inputfiles are.}

\item{overwrite}{(\emph{logical}) If set to TRUE, existing blocs of the same type
(input file extension) will be overwritten. In order to add several blocs of
the same type, the option needs to be set to FALSE.}
}
\value{
returns an instance of \emph{ogs5} with the new input blocs added
as instances of their respective class.
}
\description{
Function to add input files directly from ogs5 input files
to an instance of class  \emph{ogs5}.
}
\details{
If \code{filename} is set to "all", the whole folder specified in
\code{file_dir} will be searched for possible input files excluding \verb{*.tec},
\verb{*.vtu} \verb{*.vtk} and \verb{*.pvd} files. However, the presence of files that are
not ogs5 input files such as for example \verb{*.log} files might have the
function crashing. For filenames that are different from \code{sim_basename}, blocs of
the class \code{ogs5_additional} are added.
}
\examples{
\dontrun{
tmp <- tempdir()
ogs5_obj <- create_ogs5(sim_name = "decal", sim_id = 1L,
                        sim_path = paste0(tmp, "/decal"))

ogs5_obj <- input_add_blocs_from_file(ogs5_obj = ogs5_obj,
                       sim_basename = "decal",
                       filename = "decal.mfp",
                       file_dir = "inst/extdata/ogs5_benchmarks/ConcreteCrack",
                       overwrite = FALSE)
}
}
